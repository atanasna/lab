apiVersion: 1

datasources:
  - name: Prometheus2
    uid: Prometheus2
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "60s"
      queryTimeout: "60s"
      httpMethod: POST
