apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: watch

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: alertmanager-config
    files:
      - alertmanager.yaml=configs/alertmanager.yaml
