apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: data

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: redis-config
    files:
      - redis.conf=configs/redis.conf

