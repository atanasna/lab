apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
stringData:
  api-token: KBDBd2QODeac_imJmXvfLNNPtvXMYaZHc7xtKcnF
---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: atanasna@gmail.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
    - dns01:
        cloudflare:
          apiTokenSecretRef:
            name: cloudflare-api-token
            key: api-token
# ---
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: the-drill-eu-wildcard
#   namespace: kube-system  # Same namespace as Traefik
# spec:
#   secretName: the-drill-eu-wildcard-tls
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   commonName: "*.thedrill.eu"
#   dnsNames:
#   - "*.thedrill.eu"
#   - "thedrill.eu"
